# Options for test_mfma.cu. See test_diag.opt for the format.
#
# This one is a real bug rather than a missing feature. reprocheck used to
# count it as a skip and say nothing, which is how it stayed quiet.
xfail --amdgpu  verify rejects VGPR in scalar source of v_mfma
